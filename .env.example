# Copy to .env.local and set your private deploy key (min 32 chars).
# Never commit .env.local — it is gitignored.
# Also add the same value as SATCRAFT_DEPLOY_KEY in GitHub repo Secrets.
VITE_SATCRAFT_DEPLOY_KEY=
